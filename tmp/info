Update these files after Android 2.3.0 & iOS 1.3.0 rollout

Three artifacts ready in tmp/ for you to copy to GitHub Pages:

  - BullionView_Import_Template.csv — v2 example with all three sections (1 user spec, 1 vault with notes, 5 holdings including one referencing the user spec). Plain text the importer reads directly. Replaces the current import-template.csv.
  - BullionView_Import_Template.xlsx — the styled Excel template with section markers, dropdowns on every enum column, live auto-counting metadata formula. New artifact — link it from the guide.
  - import-guide.md — full v2 rewrite. Documents the stacked-section structure, USER_SPECS columns (15), VAULTS columns (3), the spec-overwrite-on-import rule, the "weight = pure metal content" convention, Excel padding tolerance, plus the original sections (catalogue subtypes, FX limitation, common pitfalls, tips). Replaces the current import-template (rendered as HTML) page.

  GitHub Pages upload checklist:
  1. Replace bullionvault70-cell.github.io/bullionvault/import-template.csv with the new .csv.
  2. Upload BullionView_Import_Template.xlsx to the same directory so the guide's link resolves.
  3. Replace the page rendered at bullionvault70-cell.github.io/bullionvault/import-template (probably import-template.md) with import-guide.md's content.
  
